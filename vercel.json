{
    "version": 2,
    "builds": [
        {
            "src": "server.ts",
            "use": "@now/ts-node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server.ts"
        }
    ]
}